import { Navbar as RSNavBar } from "reactstrap"

export default function Navbar() {
    return (
        <RSNavBar color={"secondary"} className="border-bottom border-primary">
            test
        </RSNavBar>
    )
}